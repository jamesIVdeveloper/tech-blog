<div class="comments">
  {{#each this}}
    <section class="comment">
      <div class="comment-text">
        {{comment_text}}
      </div>
      <div class="comment-by">
        {{user.username}}
        on
        {{format_date created_at}}
      </div>
    </section>
  {{/each}}
</div>