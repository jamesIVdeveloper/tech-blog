<div class="comments">
  {{#each this}}
    <section class="comments">
      <div class="comment-by">
        {{user.username}}
        on
        {{format_date created_at}}
      </div>
      <div class="comment-text">
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>